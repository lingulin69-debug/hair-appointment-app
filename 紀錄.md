# AMY.SALON 問題紀錄

最後更新：2026-05-14

## 基礎資訊

- 發布網站：https://vercel.com/lingulin69-4776s-projects
- 正式站：https://hair-appointment-app-nine.vercel.app
- Firebase 後台：https://console.firebase.google.com/u/0/project/hair-1b310/overview
- 注意：帳號密碼不要寫進 Git；此檔只記問題、現象、解法與驗證結果。

## 2026-05-13 問題與解法

### 1. 同頁登出再登入後誤進 bootstrap / 出現 permission-denied
- 現象：同一頁先登出再登入時，畫面有機率先撞到舊的 Firestore listener 狀態，出現假 bootstrap 或 permission-denied。
- 原因：Firestore persistent cache 與 listener 還掛在舊 auth session 上，重新登入後沒有完整重建監聽。
- 解法：在登入成功後直接執行 window.location.reload()，讓整個前端用新 session 重新建 listener。
- 驗證：本機與正式站都已驗證登入後不需要手動刷新即可正常進系統。

### 2. 正式站與本機功能不一致，看不到最新登入 / 權限 / 報表變更
- 現象：本機已完成權限與登入分流，但正式站仍顯示舊版畫面。
- 原因：Vercel 當時提供的是舊 build，正式 alias 沒有更新到最新版本。
- 解法：在專案目錄重新確認 link 後執行 vercel deploy --prod --yes，讓正式 alias 指向新部署。
- 驗證：正式站 https://hair-appointment-app-nine.vercel.app 已重新部署，owner 帳號可登入並看到新版本畫面。

### 3. 使用者反映「新增預約無法新增」
- 現象：新增預約視窗能打開，但使用者看到儲存按鈕是灰的，以為系統壞掉。
- 原因：按鈕原本在必填條件未滿足時會直接 disabled，卻沒有說明原因。最常見是沒有填顧客姓名；其他可能是服務資料同步中、沒有可用服務、當天沒有可用時段。
- 解法：在 src/components/Calendar/NewApptModal.tsx 新增 saveBlockedMessage，直接顯示目前不能儲存的原因，並讓按鈕禁用條件與提示共用同一套判斷。
- 驗證：正式站已實測顯示「請先輸入顧客姓名，才能儲存預約。」，且填入姓名後可正常儲存。

### 4. 刪除預約以為沒成功
- 現象：點一次「刪除預約」後，如果沒有做第二步確認，資料看起來還在，容易誤判成刪除失敗。
- 原因：預約詳情 Modal 採兩步驟刪除，先點刪除，再點確認刪除。
- 解法：操作刪除時要完成第二步確認；驗證時用當日時段表檢查已占用格數是否回到 0。
- 驗證：本輪測試用預約已完成二次確認並刪除，2026-05-28 已回到 64 格可預約、0 格已占用。

### 5. 手機板打開時沒看到登入頁
- 現象：手機重新打開正式站時，直接進入系統，看起來像登入頁不見了。
- 原因：同一台裝置如果還保留 Firebase 登入 session，就會直接進系統。這不是登入頁壞掉，而是 session persistence 正常生效。記住裝置的勾選只負責自動帶入帳密；真正讓畫面直接進系統的是尚未登出的登入狀態。
- 解法：若要重新看到登入頁，先在該裝置點一次登出；若仍異常，再清該站點資料或換無痕視窗重開。
- 驗證：正式站已實測，登入狀態下會直接進系統；點登出後重新整理，會穩定回到登入頁。

### 6. 改成每次重新開站先看到登入頁，但 Android 登入頁一度滑不動
- 現象：需求改成同一台裝置每次重新開網站都要先看到登入頁，但爸爸的 Android 手機出現另一個問題：登入表單要往下才看得到，頁面卻無法往下滑；一般模式與無痕模式都一樣。
- 原因：
	1. Firebase 預設會保留跨次開站的登入 session，所以原本重新開站會直接進系統。
	2. LoginScreen 在手機版內容高度超過 viewport 時，外層仍用了 overflow hidden 與垂直置中，Android 小螢幕就會變成內容超出卻不能滑。
- 解法：
	1. 登入流程改成只保留本次分頁 session；重新開新頁面時會先回登入頁。
	2. 「記住這台裝置」只保留帳號密碼自動帶入，不再保留跨次開站自動進系統。
	3. LoginScreen 改成外層自己可 overflow-y-auto，且手機版改為從上往下排，不再強制整頁垂直置中。
- 驗證：
	1. 正式站已實測，先登入一次後重新開新頁面，會回到登入頁，且帳號密碼會自動帶入。
	2. src/components/Auth/LoginScreen.test.tsx 通過。
	3. npm run build 通過。
	4. 修正已重新部署到正式站。

## 2026-05-14 問題與解法

### 1. 顧客目錄頁可以進入，但資料卡清單在手機上滑不動
- 現象：切到「顧客目錄」後可以看到資料卡，但內容超出一頁時，手指往上滑沒有反應，看起來像整頁被鎖住。
- 原因：主站 body 仍維持 overflow hidden，只允許中間主內容區自己捲動；但 src/App.tsx 的主內容 flex 鏈缺少 min-h-0，造成某些頁面在手機上拿不到正確的可縮高度，最後變成內容超出卻無法觸發內部捲動。
- 解法：在 src/App.tsx 的主內容外層與真正負責捲動的內容容器都補上 min-h-0，讓 overflow-y-auto 能正確接管捲動。
- 驗證：本機預覽已確認顧客目錄清單恢復可往下滑；npm run build 通過。

### 2. 預約統計頁的權限管理卡片在手機上文字超出框架
- 現象：進入「預約統計」頁後，長 email 帳號與管理員帳號膠囊在窄螢幕會把卡片撐寬，看起來像文字衝出框外。
- 原因：src/components/Auth/AccessControlPanel.tsx 的 email 文字區原本沒有 break-all / min-w-0 這類窄版保護，header 右上角的管理員帳號膠囊也沒有為手機寬度預留斷行行為。
- 解法：讓管理員帳號膠囊可在手機版換行，並把每張權限卡的左側資訊區改成可縮小的 flex 區塊，email 文字改為 break-all，避免把右側按鈕列往外擠。
- 驗證：本機預覽已修正窄版超框；npm run build 通過。

### 3. 顧客詳情展開視窗打開後內容超高但無法下滑
- 現象：顧客目錄清單本身已可滑動，但點進某位顧客後，展開的顧客資訊視窗在手機上仍然無法往下滑。
- 原因：src/components/Client/ClientDetailModal.tsx 只有 max-h-[92dvh] 的高度限制，卻沒有真正的內部滾動區；在 body 已被鎖住的前提下，modal 內容一旦超高，就會整塊卡住。
- 解法：把 ClientDetailModal 改成直向 flex 結構，並新增一個 flex-1 的 custom-scrollbar 內容區，讓中間資料卡區獨立 overflow-y-auto，底部操作按鈕留在面板下方。
- 驗證：本機預覽已套用修正；npm run build 通過。

## 建議後續維護原則

1. 只要本機和正式站表現不一致，先檢查是不是正式站還在跑舊 build，不要先懷疑 Firebase 資料壞掉。
2. 只要遇到「按鈕不能按」的表單回報，先檢查是不是靜默 disabled，優先補 inline 提示，而不是只靠 console 查錯。
3. 有使用瀏覽器操作刪除資料時，要確認是否有第二步確認，不要只點第一下就重整頁面。
4. 行動版登入頁只要有新增內容，優先檢查 Android 小螢幕是否仍可上下滑動；不要只用桌機或 iPhone 判斷版面正常。
5. 只要版面採用「body 鎖住、由中間內容區或 modal 內部自行捲動」的結構，新增頁面或彈窗時都要優先檢查 flex 鏈是否有 min-h-0，以及內容區是否真的有 overflow-y-auto；否則最容易在 Android 或窄版螢幕出現內容超出卻滑不動。
6. 只要卡片內會顯示 email、帳號、備註這類長字串，手機版要先補 min-w-0、break-all 或 break-words，避免單一欄位把整張卡片撐出框外。