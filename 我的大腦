# AI 知識自動歸檔與分類規範

當我完成一項任務、修復一個錯誤或達成某個階段目標時，請主動執行以下歸檔動作：


### 1. 寫入格式
更新時請採用「追加」方式，並標註日期。例如：
> [2026-03-29 更新] 
> - **問題/需求**：[簡述]
> - **解決方案/規範**：[具體指令或做法]

### 2. 自動同步
請同步在根目錄的 `/我的 AI 大腦/` 資料夾中生成一份詳細的 Markdown 備份，方便我日後跨平台查看。


# 開發與架構最高守則

## 1. 輸出與溝通規範
- **省 Token 模式優先**：當優化或修改程式碼時，僅輸出受影響的函數/組件，禁止全文輸出，並使用註釋標註修改位置。
- **避免過度設計 (YAGNI)**：僅針對當前明確指定的需求實作。禁止預先加入未要求的抽象層或複雜架構。

## 2. 測試與驗證 (TDD & Red-Green)
- 不得把 `console.log(...)`、人工目視結果、口頭推論當成測試通過證據。
- 只要修改核心功能邏輯，必須提供至少一個自動化測試。
- 新增或修改測試後，必須先做一次「故意失敗驗證」，再恢復正確值重新執行。
- 若測試一開始就綠燈，不能直接宣稱完成；必須先證明該測試真的能抓到錯誤。
- 禁止提交「永遠為真」或低資訊量的斷言 (如: `expect(x).toBe(x)`)，或僅有 console.log 的假測試。
- 禁止用與真實邏輯無關的硬編碼假資料，除非該測試明確標示為 fixture / mock test，且目的已說明。

## 3. 穩定性與防禦性編碼 (Defensive Programming)
- 永遠假設外部傳入的資料（Props, API Response）是不可靠的。必須實作空值防護 (Optional Chaining, Default Props)。
- 遵守 Fail-Fast 原則：遇到異常資料提早 `return` 或拋出錯誤，配合 Error Boundary，嚴禁引發整頁白屏崩潰。

## 4. 職責分離與 React 規範 (SRP & Hooks)
- 嚴格區分 UI 展示與業務邏輯。複雜的資料狀態與 Firebase 訂閱應提升至頂層 (Lifting State Up) 或封裝為 Custom Hooks。
- 嚴格遵守 React Hook 呼叫順序與依賴陣列 (Dependencies Array) 完整性。所有副作用 (`useEffect`) 必須實作清理函數 (Cleanup)。

> [2026-03-29 更新]
> - **問題/需求**：`NewApptModal` 在日曆頁從關閉切到開啟時，會因 React Hooks 呼叫順序不一致而觸發錯誤邊界，錯誤為 `Rendered more hooks than during the previous render.`。
> - **解決方案/規範**：禁止把任何 `useMemo`、`useEffect`、`useState` 等 hooks 放在依賴 `shouldRender`、`isOpen` 或條件式 `return` 之後。若元件需要以 `return null` 控制掛載，必須先完成所有 hooks 宣告，再做早退判斷。
> - **解決方案/規範**：針對這類 Modal 掛載問題，必須補一個「先關閉 render，再開啟 rerender」的回歸測試，避免未來再次引入 Hook order bug。

> [2026-03-29 更新]
> - **問題/需求**：首頁日曆與服務/項目頁載入過慢，根因是 `App.tsx` 首屏同時啟動多條 Firestore 訂閱，且 `appointments` / `leaves` 採用全量 collection 訂閱，`Calendar` 端又重複排序與重複 `.filter()`。
> - **解決方案/規範**：Firestore hook 預設必須支援 `enabled` 或等效機制，避免在非當前頁面也持續訂閱資料。
> - **解決方案/規範**：任何與日曆視圖綁定的資料源，優先以「當前可視月份」或「實際需要日期區間」查詢，不得默認全量抓取整個 collection。
> - **解決方案/規範**：UI 端若同一份資料會在多個日期格/區塊反覆查找，必須先做分組或索引化，再交給畫面渲染，避免 render 階段反覆掃描整個陣列。

> [2026-03-29 更新]
> - **問題/需求**：即使縮小查詢範圍後，日曆與服務/商品頁的體感載入仍然偏慢，表示瓶頸不只在查詢範圍，也包含首次遠端資料等待與首包體積。
> - **解決方案/規範**：低頻資料頁面（如服務/商品、休假、月份預約）優先採用 cache-first 策略：先讀本地快取立即顯示，再用 `getDocs()` 背景刷新，不應把 `onSnapshot()` 當成首屏唯一資料來源。
> - **解決方案/規範**：若 CRUD 行為由同一前端控制，新增/修改/刪除成功後應直接更新本地 state 與快取，避免畫面必須等待下一次遠端同步才反映結果。
> - **解決方案/規範**：Firebase Web 專案若以 Firestore 為核心資料源，應優先評估 `persistentLocalCache` 作為預設初始化策略，以提升第二次進頁與切頁體感速度。
> - **解決方案/規範**：首頁主包若過大，應優先用 lazy loading 與 `manualChunks` 拆出 `react` / `firebase` / 圖示庫等共用依賴，避免所有頁面與依賴都綁在單一入口檔。

> [2026-03-29 更新]
> - **問題/需求**：若 loading 狀態把整個主畫面換成 skeleton，使用者會主觀認為「頁面還沒出現」，即使實際上只是資料尚未回來。
> - **解決方案/規範**：Calendar、清單頁、管理頁等主畫面應優先 render 真實框架與主要版面，只把資料區塊標示為 syncing/loading，避免 loading 狀態遮蔽整頁。
> - **解決方案/規範**：對效能問題不可只靠體感判斷，應在關鍵資料 hook 中加入可重複驗證的載入時間紀錄，例如以 `performance.now()` 量測 `getDocs()` 或初始化流程耗時。

> [2026-03-29 更新]
> - **問題/需求**：日曆實際上不是完全沒載入，而是 `Calendar.tsx` 在 `isLoading` 時整個月曆區塊直接改渲染成 skeleton，服務/商品頁也有相同模式，導致使用者主觀感受成「畫面 20 秒後才出現」。
> - **解決方案/規範**：主畫面型元件不得在 loading 期間整塊替換為 skeleton，必須優先 render 真實版面骨架，例如月曆格線、頁面標題、篩選列、操作按鈕；loading 只能作用在資料區塊或以同步提示呈現。
> - **解決方案/規範**：當使用者回報「很慢」時，要先區分是「資料慢」還是「UI 把自己藏起來」，兩者修法不同；前者處理資料來源，後者處理 render 策略。
> - **解決方案/規範**：若 render-first 修正後體感明顯改善，表示關鍵問題在 loading 呈現方式而非單純網路速度；此類頁面後續一律採 render-first + background sync。
