# AMY.SALON 交接文檔

更新日期：2026-05-13

## 1. 專案現況

- 專案名稱：AMY.SALON 美髮院管理系統
- GitHub：https://github.com/lingulin69-debug/hair-appointment-app.git
- 正式站：https://hair-appointment-app-nine.vercel.app
- Firebase 專案：hair-1b310
- 前端技術：React 18 + TypeScript + Vite
- 資料層：Firebase Authentication + Firestore
- 快取策略：Firestore persistentLocalCache + localStorage 快取
- 部署平台：Vercel

## 2. 目前已可用功能

### 前台 / 後台分流
- 前台模式：日曆排程、月曆、多日排程、當日時段表、預約操作、完成結帳。
- 後台模式：顧客管理、服務與商品、庫存異動、收入報表、權限管理。
- Navbar 已改為先切前台 / 後台，再切對應頁面。

### 登入與權限
- 主站已加入 Firebase Authentication 真正登入 gate，不再只是把畫面藏起來。
- 角色模型已固定為 admin / owner / staff。
- admin：最高權限，可看權限管理面板、可操作匯入工具。
- owner：可進前台與後台，但看不到權限管理面板，避免誤改權限。
- staff：只能進前台。
- 登入支援短帳號自動補成 @amysalon.local。
- 可勾選記住這台裝置的帳號密碼。
- 目前登入策略已改成：重新開新頁面時一律先回登入頁，但若有勾選記住裝置，帳號密碼會自動帶入，不用重打。
- 同頁登出再登入時，登入成功後會直接 reload，避免假 bootstrap / permission-denied 狀態殘留。
- LoginScreen 已補 Android 小螢幕可捲動修正，避免登入表單超出畫面卻不能下滑。

### 預約 / 顧客 / 結帳 / 庫存
- 月曆與當日時段表可直接新增、查看、編輯、刪除預約。
- 預約詳情可直接完成結帳。
- 顧客頁與顧客詳情已可顯示最近一次消費與累積總消費。
- 商品頁已接上庫存摘要與庫存異動視窗。
- 匯入工具 import.html / src/importer.ts 已改成登入後才能操作，且只有 admin 可匯入。

### Dashboard / 收入統計
- Dashboard 收入口徑維持：未取消預約收入 + 手動記帳。
- 已有最近 14 日日結餘、週結算、月結算。
- 月份總覽可點進去查看當月逐筆紀錄。

## 3. 2026-05-13 本輪完成重點

- 已完成 admin / owner / staff 權限升級與畫面分流。
- 已把爸媽帳號調整為 owner 同級，權限面板只留給 admin。
- 已完成短帳號登入與記住裝置帳密。
- 已完成「重新開站先回登入頁，但帳密仍可自動帶入」的登入行為調整。
- 已修正同頁重新登入時的假 bootstrap 問題。
- 已完成 Dashboard 月份點擊明細與區間結算 UI。
- 已檢查「新增預約無法新增」問題，根因不是寫入壞掉，而是必填條件未滿足時按鈕原本會靜默禁用。
- 已在新增預約視窗加入明確提示訊息，正式站會直接顯示不能儲存的原因。
- 已修正 Android 手機登入頁內容超高但無法下滑的問題。
- 已重新部署正式站並驗證最新版本已上線。

## 4. 驗證結果

- npm test -- --environment jsdom src/components/Auth/LoginScreen.test.tsx src/App.auth.test.tsx：通過。
- npm run build：通過。
- 正式站已驗證：
  - 重新開新頁面時會先回登入頁。
  - 若先前勾選記住裝置，登入頁會自動帶入帳號密碼。
  - owner 帳號可登入。
  - owner 可進前台與顧客頁。
  - owner 看不到權限管理面板。
  - 新增預約視窗在未填顧客姓名時會顯示「請先輸入顧客姓名，才能儲存預約。」

## 5. 接手時先看哪些檔案

- src/App.tsx：主入口，前後台分流、登入 gate、預約儲存、結帳與 modal 狀態都在這裡。
- src/hooks/useAuth.ts：登入 / 登出與 session 狀態。
- src/hooks/useAccessControl.ts：角色解析、bootstrap、權限清單。
- src/components/Calendar/NewApptModal.tsx：新增 / 編輯預約表單，包含儲存禁用原因提示。
- src/components/Dashboard/Dashboard.tsx：收入、日結餘、週結算、月結算、月份點擊明細。
- src/importer.ts、import.html：admin-only 匯入流程。
- 紀錄.md：看最近遇到的問題與解法。

## 6. 目前待接手建議

1. 持續用真實店務流程驗證結帳、交易、庫存異動是否完全符合現場操作。
2. 若使用者仍常反映「按鈕灰掉不知道原因」，可把更多表單都改成像新增預約一樣，直接顯示 inline 提示。
3. 帳號密碼不要寫入 Git 或任何版本控制檔案，只保留帳號權限架構與角色說明。
