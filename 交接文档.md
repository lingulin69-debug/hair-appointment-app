# AMY.SALON 交接文檔

更新日期：2026-05-31

## 1. 一頁摘要

- 專案名稱：AMY.SALON 美髮院管理系統
- GitHub：https://github.com/lingulin69-debug/hair-appointment-app.git
- 目前主分支：master
- 最新上線 commit：69d278f (`Complete salon app updates`)
- 正式站：https://hair-appointment-app-nine.vercel.app
- 本次 deployment：https://hair-appointment-oh1ml1m5g-lingulin69-4776s-projects.vercel.app
- Firebase 專案：hair-1b310
- 技術棧：React 18 + TypeScript + Vite + Tailwind CSS + Firebase Authentication + Firestore
- 快取策略：Firestore persistentLocalCache + localStorage
- 專案狀態：主要店務流程已可用，並已推送 GitHub 與部署到 Vercel production

## 2. 目前已完成功能

### 2-1. 登入與權限

- 主站有真正的 Firebase Authentication 登入 gate。
- 角色模型已固定為 `admin / owner / staff`。
- `admin` 可看權限管理與匯入工具；`owner` 可用前後台；`staff` 只可進前台。
- 登入支援短帳號自動補成 `@amysalon.local`。
- 可記住裝置上的帳號密碼；重新開新頁時會回登入頁，但可自動帶回已記住帳密。
- 同頁登出再登入後會主動 reload，避免舊 listener 造成假 bootstrap / permission-denied。
- Android 小螢幕登入頁已修正為可正常往下滑。

### 2-2. 前台排程 / 預約流程

- 日曆提供 `月曆 / 多日排程` 雙模式。
- 多日排程支援整月橫向拖動、08:00-24:00 時段顯示、自訂日期跳轉月曆彈窗。
- 點日期可看當日時段表與空檔；可直接新增預約。
- 預約可新增、編輯、取消；取消後不再佔用時段。
- 預約詳情可查看日期、時間、服務、人數、金額、備註。
- 2026-05-31 起，從多日排程或月曆點開預約後，可直接看到手機號碼並使用「撥打電話」。
- 若舊預約文件本身沒有 `phone`，詳情視窗會優先從 `clientId / clientName` 對應顧客資料補齊電話。
- 預約詳情可直接進入完成結帳流程。

### 2-3. 顧客 / 結帳 / 交易

- 顧客可新增、編輯、刪除、搜尋。
- 顧客詳情可直接撥號。
- 顧客詳情會顯示最近一次消費、累積總消費與逐筆交易歷史。
- 月曆預約可完成結帳，結帳後會建立 `transactions`，並把預約標成 completed。
- 交易摘要已做去重邏輯，避免同筆資料重複累加。
- 顧客資料可匯出 Excel，輸出 `顧客資料` 與 `消費紀錄` 兩張工作表。

### 2-4. 服務 / 商品 / 庫存

- 服務與商品可新增、編輯、刪除。
- 商品頁已接上庫存摘要與庫存異動。
- 結帳中的商品 line item 會同步寫入 `inventoryMovements`。
- 多日排程色塊時長會優先取 `storeItems.duration`，無對應時 fallback 60 分鐘。

### 2-5. Dashboard / 收入

- Dashboard 已有預約統計、收入相關檢視與月份明細。
- 收入口徑包含預約收入與手動記帳。
- 已有最近 14 日日結餘、週結算、月結算。

### 2-6. 資料救援 / 匯入工具

- `recovery.html` / `src/recovery.ts` 可從 localStorage 與 Firestore cache 匯出救援 JSON。
- 已處理 LINE 內建瀏覽器不能直接下載 JSON 的情境，提供 inline 顯示、複製、分享與分段複製備援。
- `import.html` / `src/importer.ts` 可把備份 JSON 匯回 Firebase。
- 匯入工具目前需要登入，且只有 `admin` 可操作。

## 3. 本輪前的重要里程碑

### 2026-05-08

- 完成前台 / 後台骨架。
- 打通月曆結帳最短路徑，建立 `transactions` 與 `inventoryMovements`。
- 顧客頁開始顯示交易資料，而不是只看 appointments。

### 2026-05-13

- 權限模型升級為 `admin / owner / staff`。
- 補齊短帳號登入、記住裝置、重新開站先回登入頁的行為。
- 正式站重新部署並驗證 owner 可正常登入與操作。

### 2026-05-14

- 修正刪除 / 取消流程的假成功問題。
- `取消此預約` 已改為真正標成 `cancelled`，不再硬刪資料。
- 手機版顧客列表、顧客詳情、權限卡片捲動與超框問題已修正。

### 2026-05-22

- 修正 cancelled 預約仍佔用時段的問題。
- 顧客詳情 modal 新增交易歷史。
- 顧客資料新增 Excel 匯出。
- 完成交易去重與短時間重複結帳防呆。

### 2026-05-31

- 多日排程 / 月曆點開預約後可看到電話並可直接撥號。
- 已把目前專案狀態推送到 GitHub，並完成 production 部署。

## 4. 目前驗證狀態

- `npm run build`：2026-05-31 通過。
- 本機 preview：`http://127.0.0.1:4173/` 可正常回應 `HTTP 200`。
- GitHub：已推送到 `origin/master`。
- Vercel production：已成功部署，正式網址 alias 正常。

### 測試現況

- 目前 Vitest 存在既有 suite 初始化異常。
- 現象是多個未修改測試也會出現 `Cannot read properties of undefined (reading 'config')`，或 `Vitest failed to find the current suite`。
- 因此目前可依賴的自動驗證以 `npm run build` 為主；若要恢復單測可信度，建議先處理 Vitest 設定 / test setup 問題。

## 5. 接手時優先看哪些檔案

- `src/App.tsx`
  - 主入口；前後台切換、登入 gate、預約詳情、結帳、客戶 / 商品 / modal 狀態都在這裡。
- `src/hooks/useAuth.ts`
  - 登入、登出、session 行為。
- `src/hooks/useAccessControl.ts`
  - 角色解析、bootstrap、權限管理。
- `src/components/Calendar/Calendar.tsx`
  - 月曆與多日排程入口。
- `src/components/Calendar/MultiDaySchedule.tsx`
  - 多日排程本體、日期跳轉、自動排版色塊。
- `src/components/Calendar/AppointmentDetailModal.tsx`
  - 預約詳情、電話顯示與撥號入口。
- `src/components/Calendar/NewApptModal.tsx`
  - 新增 / 編輯預約。
- `src/components/Client/ClientDetailModal.tsx`
  - 顧客詳情、交易歷史、電話撥打。
- `src/components/Client/ClientList.tsx`
  - 顧客清單與匯出。
- `src/utils/clientExport.ts`
  - 顧客 Excel 匯出。
- `src/importer.ts`、`src/recovery.ts`
  - 救援與匯入工具。
- `紀錄.md`
  - 問題排查與日常修改紀錄。
- `手機版 UI 排查 SOP.md`
  - 遇到手機版滑不動、超框、長字串擠壓時先照這份排查。

## 6. 已知風險 / 注意事項

- 單測目前不可完全信任：先修 Vitest，再談回歸測試覆蓋率。
- `vite build` 仍有大 chunk 警告（`firebase` / `xlsx` 相關 chunk 偏大），目前不阻擋上線，但後續可再做 code-splitting。
- 預約電話現在有 fallback，但如果舊 `appointment` 與 `client` 都沒電話，撥號按鈕仍會 disabled，這是預期行為。
- 本機 `backup/hair-appointment-app-main-20260523-003851.zip` 沒有推上 GitHub，仍只留在本機備份。
- 帳號密碼與任何敏感資訊不要寫進 Git；只保留權限架構與流程說明。

## 7. 建議下一步

1. 先修 Vitest suite 初始化問題，讓單測恢復可用。
2. 用真實店務流程再跑一次 `預約 -> 結帳 -> 交易 -> 庫存` 全鏈路。
3. 若要再優化體感效能，優先處理大型 chunk 與重資源 lazy load。
4. 若未來還要擴充前台操作，可優先從 `AppointmentDetailModal` 與 `MultiDaySchedule` 延伸，因為目前使用者最常操作的是這條路徑。
