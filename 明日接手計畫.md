# 美髮預約專案 明日接手計畫

更新日期：2026-04-27

## 0. 結案註記（最新）

- 本文件原本是 2026-04-23 的資料救援接手計畫，以下內容保留作歷史記錄。
- 截至 2026-04-27，主站需求已完成並重新部署到正式站：
  - `https://hair-appointment-app-nine.vercel.app`
- 多日排程視圖已完成改版：
  - 保留原本月曆模式
  - 新增多日排程模式
  - 左側時間導引已取消
  - 改為每個日期欄顯示整點時間
  - 整點字級已再放大 3px
- Firebase / Firestore 正式站已完成真實驗證：
  - 已確認正式站連到 `hair-1b310`
  - `同步資料` 按鍵可正常使用
  - 已做過真實新增顧客寫入測試，重新載入後能讀回
  - 測試資料已清除，Firestore 回查剩餘 0 筆
- 已建立本機備份：
  - `backup/hair-appointment-app-main-20260427-005153.zip`
- 已建立 AI 備份紀錄：
  - `我的 AI 大腦/2026-04-27_多日排程整點版_部署備份.md`
- **目前狀態：本專案這一輪可視為已完成，可先關閉。**
- 若未來再開啟，不要先看下面的救援步驟，請先看：
  1. `PROJECT_STATUS.md`
  2. `我的 AI 大腦/2026-04-27_多日排程整點版_部署備份.md`

## 1. 目前狀態

- 已確認正式站 Firebase 專案是 `hair-1b310`
- 已確認雲端路徑為 `artifacts/hair-1b310/public/data/*`
- 已知雲端目前 `storeItems` 有資料，但 `clients`、`appointments`、`leaves` 幾乎為空
- 已確認爸媽手機目前「仍然看得到預約資料」
- 這代表資料高機率仍存在於原手機原瀏覽器的本地快取

## 2. 今天已完成的事

### 已完成功能

- 建立獨立備份頁 `recovery.html`
- 建立備份腳本 `src/recovery.ts`
- `vite.config.ts` 已加入 `recovery.html` 多頁入口
- 已把備份頁部署到正式站
- 已確認正式站可打開：
  - `https://hair-appointment-app-nine.vercel.app/recovery.html`
  - 建議實際使用帶版本參數的網址避免舊快取：
    - `https://hair-appointment-app-nine.vercel.app/recovery.html?v=20260423c`

### 已完成頁面文案

- 標題已改成「本地資料備份」
- 字體已放大
- 字重已加粗
- 已改成「第一步、第二步、第三步、第四步、第五步」的老人友善引導

## 3. 目前最重要原則

在拿到備份 JSON 之前：

- 不要請爸媽先進首頁
- 不要叫爸媽新增或修改資料
- 不要清除瀏覽器資料
- 不要切換成別的瀏覽器
- 不要卸載瀏覽器或使用清理工具

## 4. 明天開工第一步

### 目標

先把爸媽手機裡的本地資料安全備份出來。

### 操作順序

1. 請爸媽用原本那支看得到資料的手機
2. 請爸媽用原本同一個瀏覽器
3. 打開：
   - `https://hair-appointment-app-nine.vercel.app/recovery.html?v=20260423c`
4. 確認畫面顯示「本地資料備份」
5. 先按「掃描本地資料」
6. 確認有沒有抓到 `clients` / `appointments` / `leaves` 數量
7. 再按「掃描並下載備份」
8. 取得下載下來的 JSON 檔

## 5. 明天拿到 JSON 後要做什麼

### 第一階段：先檢查備份內容

收到 JSON 後，先檢查：

1. `localStorage.entries` 是否存在
2. `localStorage.merged.clients` 是否有資料
3. `localStorage.merged.appointments` 是否有資料
4. `localStorage.merged.leaves` 是否有資料
5. `firestoreCache.clients` / `appointments` / `leaves` 是否也有資料

### 第二階段：決定匯入來源

優先順序：

1. 先看 `localStorage.merged.*`
2. 再看 `firestoreCache.*`
3. 如果兩邊都有資料，需先比對重複與數量
4. 以資料較完整者作為回灌來源

## 6. 明天的實作任務

### 任務 A：做雲端匯入工具

目標：

- 做一個一次性匯入工具，將備份 JSON 回寫到 Firebase

預計做法：

1. 新增獨立匯入頁或本地匯入腳本
2. 先讀 JSON
3. 顯示要匯入的筆數摘要
4. 使用既有 Firebase 設定回寫到：
   - `artifacts/hair-1b310/public/data/clients`
   - `artifacts/hair-1b310/public/data/appointments`
   - `artifacts/hair-1b310/public/data/leaves`
5. 匯入完成後再到 Firebase 實際確認數量

### 任務 B：修掉假成功問題

已知 bug：

- `App.tsx` 目前在儲存顧客與儲存預約時，即使寫入失敗，也會在 `finally` 關掉視窗
- 使用者會誤以為已儲存成功

明天要修的方向：

1. 只有在 `addClient` / `updateClient` / `addAppointment` 真正成功時才關閉視窗
2. 若失敗，視窗保持開啟
3. 顯示明確錯誤訊息給使用者

## 7. 明天的建議執行順序

1. 先拿到爸媽手機的 JSON 備份
2. 先檢查 JSON 結構與筆數
3. 再做匯入工具
4. 先回灌 `clients`
5. 再回灌 `appointments`
6. 再回灌 `leaves`
7. 到 Firebase 或前端實際驗證雲端資料是否存在
8. 最後才修 `App.tsx` 的假成功問題

## 8. 已知技術細節

### 本地資料來源

- Firestore 使用 `persistentLocalCache`
- 額外 localStorage 快取前綴為 `hair-salon:`
- 已知鍵名包含：
  - `hair-salon:clients`
  - `hair-salon:appointments:*`
  - `hair-salon:leaves:*`
  - `hair-salon:store-items`

### 已修改檔案

- `recovery.html`
- `src/recovery.ts`
- `vite.config.ts`

### 本地驗證結果

- `npm install` 已完成
- `npm run build` 已成功
- `dist/recovery.html` 已成功生成
- Vercel production 已成功重新部署

## 9. 部署資訊

### Vercel 專案

- 專案名稱：`hair-appointment-app`
- 正式網址：`https://hair-appointment-app-nine.vercel.app`

### 本地部署指令

```bash
npm run build
npx vercel --prod --yes
```

## 10. 明天開始時先看這段

明天如果要直接接手，請先做這三件事：

1. 先確認爸媽是否已下載 JSON
2. 如果已下載，先讀 JSON 內容再決定匯入策略
3. 不要一開始就改主站功能，先完成資料保全與回灌

## 11. 若明天要直接延續的工作提示

可直接從這裡開始：

- 先請使用者上傳或貼出 JSON 備份檔內容摘要
- 然後建立一次性 Firebase 匯入工具
- 匯入後驗證 `clients`、`appointments`、`leaves` 雲端資料是否恢復
- 再處理 `App.tsx` 的假成功儲存問題